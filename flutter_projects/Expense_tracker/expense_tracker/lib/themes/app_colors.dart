import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors
  static const lightPrimary = Color(0xFF007AFF);
  static const lightBackground = Color(0xFFF0F8FF);
  static const lightSurface = Colors.white;
  static const lightOnSurface = Color(0xFF2D2D2D);
  static const lightSecondary = Color(0xFF666666);
  static const lightHint = Color(0xFF999999);
  static const lightGradientEnd = Color(0xFFE3F2FD);
  
  // Dark theme colors
  static const darkPrimary = Color(0xFF0A84FF);
  static const darkBackground = Color(0xFF1C1C1E);
  static const darkSurface = Color(0xFF2C2C2E);
  static const darkOnSurface = Colors.white;
  static const darkSecondary = Color(0xFF8E8E93);
  static const darkHint = Color(0xFF8E8E93);
}